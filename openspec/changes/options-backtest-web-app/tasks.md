## 1. Project Scaffolding

- [x] 1.1 Create `web/` directory structure with `backend/` and `frontend/` subdirectories matching the layout in design.md. Verify all directories exist: `web/backend/routers/`, `web/backend/engine/`, `web/backend/data/`, `web/frontend/src/pages/`, `web/frontend/src/components/`, `web/frontend/src/hooks/`, `web/frontend/src/types/`.
- [x] 1.2 Create `web/requirements.txt` with Python dependencies (fastapi, uvicorn, pydantic, aiosqlite, httpx, scipy, numpy, pandas). Verify `pip install -r web/requirements.txt` succeeds.
- [x] 1.3 Initialize React frontend with Vite + TypeScript template in `web/frontend/`. Install dependencies: recharts, date-fns, react-datepicker. Verify `npm run build` produces output in `web/frontend/dist/`.
- [x] 1.4 Create `web/backend/main.py` with FastAPI app, CORS middleware (allow localhost origins), and static file mounting for the frontend dist. Verify `python -m uvicorn web.backend.main:app` starts and serves a health check at `GET /api/health`.
- [x] 1.5 Configure Vite dev server proxy to forward `/api/*` requests to the FastAPI backend (port 8000). Verify `npm run dev` frontend can call `/api/health` through the proxy.

## 2. SQLite Cache Layer

- [x] 2.1 Create `web/backend/data/models.py` defining SQLite tables: `ohlcv_candles` (instrument, date, interval, open, high, low, close, volume), `option_premiums` (instrument, date, expiry, strike, option_type, open_premium, close_premium, high_premium, low_premium), `intraday_candles` (instrument, date, strike, option_type, expiry, time, open, high, low, close), `sync_log` (instrument, start_date, end_date, synced_at, status). Verify tables are created with correct indices on (instrument, date) and (instrument, date, strike, option_type).
- [x] 2.2 Create `web/backend/data/cache.py` with async functions: `init_db()` (creates tables if not exist), `insert_ohlcv()`, `insert_option_premiums()`, `get_ohlcv(instrument, date)`, `get_option_premium(instrument, date, strike, option_type, expiry)`, `get_synced_date_range(instrument)`, `get_missing_dates(instrument, start, end)`. Verify a round-trip test: insert sample OHLCV row, query it back, confirm values match.
- [x] 2.3 Add `saved_backtests` table to the SQLite schema with columns: id (UUID), strategy_config (JSON), backtest_config (JSON), results (JSON), created_at. Verify insert and retrieval of a saved backtest by id.

## 3. Upstox Historical Data Fetcher

- [x] 3.1 Create `web/backend/data/upstox_fetcher.py` with a class `UpstoxHistoricalFetcher` that extends credential loading from the existing `upstox_client.py`. Add method `fetch_index_ohlcv(instrument_key, from_date, to_date, interval)` calling Upstox v2 `/historical-candle/{instrumentKey}/{interval}/{to_date}/{from_date}`. Verify it returns parsed OHLCV records for NIFTY 50 with a valid access token.
- [x] 3.2 Add method `fetch_instruments_master()` to download and parse the Upstox instruments master CSV from `https://assets.upstox.com/market-quote/instruments/exchange/complete.csv.gz`. Filter for `exchange="NSE_FO"`, `instrument_type="OPTIDX"`, `option_type` in ("CE", "PE"), and `name` in ("NIFTY", "BANKNIFTY"). Build a lookup dict mapping `(underlying, strike, expiry, option_type)` → `instrument_key` using the `strike` column (not `strike_price`). Verify lookup resolves a NIFTY 24000 CE weekly expiry to the correct instrument key.
- [x] 3.3 Add method `fetch_option_historical(instrument_key, from_date, to_date, interval)` to fetch historical candles for individual option contracts. Verify it returns OHLCV data for a known option instrument key.
- [x] 3.4 Add method `sync_data(underlying, start_date, end_date, progress_callback)` that orchestrates: fetch index OHLCV for missing dates → load instruments master → for each trading day compute ATM → dynamically resolve nearest expiry from instruments (not hardcoded Thursday) → resolve relevant strikes → fetch option premiums for all trading days → store in SQLite cache. Implement rate limiting (250ms between calls) and 429 retry with backoff. Verify sync completes for a 1-week date range and data appears in cache.
- [x] 3.5 Implement incremental sync logic: check `get_missing_dates()` to identify dates without OHLCV data, but always fetch option premiums for all trading days in the requested range (even if OHLCV exists). This ensures option data is populated when re-syncing. Verify that re-running sync fetches missing option premiums without redundantly fetching OHLCV.

## 4. Pydantic Schemas

- [x] 4.1 Create `web/backend/schemas.py` with Pydantic models: `StrategyLeg` (position: buy/sell, option_type: CE/PE, strike_offset: int, lots: int), `StrategyDefinition` (underlying: NIFTY/BANKNIFTY, expiry_type: weekly/monthly, legs: list[StrategyLeg]), `BacktestConfig` (start_date, end_date, entry_time, exit_time, stop_loss_enabled, stop_loss_mode, stop_loss_value, target_profit_enabled, target_profit_mode, target_profit_value, trailing_sl_enabled, trailing_sl_value, vix_min, vix_max, weekdays: list[int]). Verify Pydantic validation rejects invalid inputs (e.g., empty legs, start_date > end_date).
- [x] 4.2 Add response models: `BacktestSummary`, `TradeRecord`, `BacktestResult` (summary, trades, equity_curve, drawdown, monthly_pnl), `TaskStatus` (task_id, status, progress, result). Verify serialization to JSON produces expected structure.

## 5. Backtest Engine (Web-Adapted)

- [x] 5.1 Create `web/backend/engine/pricing.py` with a `get_option_price(instrument, date, strike, option_type, expiry, time_of_day)` function that first queries the SQLite cache for real historical premiums, falling back to Black-Scholes estimation (importing from `option_backtester.pricing`) if no data found. Return includes a `source` field ("real" or "estimated"). Verify it returns real price for cached data and estimated price for missing data.
- [x] 5.2 Create `web/backend/engine/simulator.py` with function `run_backtest(strategy: StrategyDefinition, config: BacktestConfig) -> BacktestResult`. Implement the day-by-day simulation loop: for each trading day in range, filter by weekday and VIX, price all legs at entry time, track intraday P&L using 5-min candle data or high/low, apply SL/TP/trailing SL exit rules, price legs at exit, record trade. Verify a Bull Call Spread backtest over 5 known days produces expected P&L values.
- [x] 5.3 Implement stop loss logic in the simulator: check combined strategy P&L against threshold at each intraday checkpoint. Support both percentage-of-premium and absolute-points modes. Verify SL triggers correctly on a day where the strategy exceeds the loss threshold.
- [x] 5.4 Implement target profit logic: same structure as SL but for profit threshold. Verify TP triggers correctly when strategy profit exceeds threshold.
- [x] 5.5 Implement trailing stop loss: once position is in profit, adjust SL upward by (current_profit - trail_distance). Verify trailing SL activates after initial profit and triggers on pullback.
- [x] 5.6 Implement summary statistics calculation: total_pnl, num_trades, win_rate, max_drawdown (points and %), sharpe_ratio (annualized, assuming 252 trading days), avg_win, avg_loss, profit_factor, max_consecutive_wins, max_consecutive_losses, monthly_pnl breakdown, equity_curve, drawdown series. Verify Sharpe ratio and max drawdown values against a manually calculated example.

## 6. API Routers

- [x] 6.1 Create `web/backend/routers/backtest.py` with `POST /api/backtest` that validates the request body (StrategyDefinition + BacktestConfig), starts the backtest as a background task, returns HTTP 202 with task_id. Add `GET /api/backtest/{task_id}/status` that returns task progress and result when complete. Verify end-to-end: POST request → get task_id → poll status → receive results.
- [x] 6.2 Add `GET /api/backtest/{backtest_id}` for retrieving saved backtests from SQLite. Add save logic: after a backtest completes, store the result. Verify save and retrieval returns identical data.
- [x] 6.3 Create `web/backend/routers/data.py` with `POST /api/data/sync` that accepts underlying, start_date, end_date and starts a background data sync task. Add `GET /api/data/sync/{task_id}/status` for progress polling. Add `GET /api/data/status` returning cache state per underlying. Verify sync endpoint starts the fetcher and status endpoint reports progress.
- [x] 6.4 Create `web/backend/routers/instruments.py` with `GET /api/instruments/underlyings` (returns NIFTY and BANKNIFTY metadata including lot_size and strike_interval), `GET /api/instruments/{underlying}/expiries` (returns weekly/monthly expiry dates from cached data), `GET /api/instruments/{underlying}/strikes` (returns available strikes for a given date and expiry). Verify all three endpoints return correct data from the cache.
- [x] 6.5 Implement in-memory task registry in `web/backend/tasks.py`: dict mapping task_id to {status, progress, result, error}. Used by backtest and data sync routers. Verify concurrent tasks don't interfere with each other.

## 7. Frontend — TypeScript Types & API Client

- [x] 7.1 Create `web/frontend/src/types/backtest.ts` with TypeScript interfaces matching Pydantic schemas: `StrategyLeg`, `StrategyDefinition`, `BacktestConfig`, `BacktestSummary`, `TradeRecord`, `BacktestResult`, `TaskStatus`. Verify types compile without errors.
- [x] 7.2 Create `web/frontend/src/api/client.ts` with functions: `runBacktest(strategy, config)`, `pollBacktestStatus(taskId)`, `getSavedBacktest(id)`, `syncData(underlying, startDate, endDate)`, `pollSyncStatus(taskId)`, `getDataStatus()`, `getUnderlyings()`, `getExpiries(underlying)`, `getStrikes(underlying, date, expiry)`. Verify at least one function calls the backend successfully in dev environment.

## 8. Frontend — Strategy Builder Component

- [x] 8.1 Create `web/frontend/src/components/StrategyBuilder/StrategyBuilder.tsx` with underlying dropdown (NIFTY/BANKNIFTY), expiry type toggle (Weekly/Monthly), and "Add Leg" button. State managed via React useState. Verify the component renders with default values (NIFTY, Weekly).
- [x] 8.2 Create `LegRow.tsx` component displaying a single leg with dropdowns: position (Buy/Sell), option type (CE/PE), strike offset (ATM-relative, generated from strike interval), lots (numeric input 1-50). Include a remove button. Verify adding a leg shows all fields and changing values updates parent state.
- [x] 8.3 Implement preset strategy templates dropdown (Iron Condor, Iron Butterfly, Bull Call Spread, Bear Put Spread, Straddle, Strangle, Short Straddle, Short Strangle). Selecting a preset populates legs matching the existing `strategies.py` definitions. Verify selecting "Iron Condor" creates 4 correct legs.
- [x] 8.4 Implement strategy summary panel showing: total legs count, net position indicator (net buyer/seller), leg breakdown list. Summary updates in real-time as legs are modified. Verify summary reflects current leg state after adding, removing, or editing legs.
- [x] 8.5 Add validation: max 8 legs, min 1 leg (cannot remove last), disable "Add Leg" at max. Display validation errors on the Run Backtest button if no legs. Verify all constraints are enforced in the UI.

## 9. Frontend — Backtest Config Component

- [x] 9.1 Create `web/frontend/src/components/BacktestConfig/BacktestConfig.tsx` with date range picker (start and end dates), entry time picker (default 09:20), exit time picker (default 15:15). Validate: max 2 years range, start < end, entry_time < exit_time. Verify defaults render correctly and validation prevents invalid inputs.
- [x] 9.2 Add stop loss toggle and configuration: mode switch (percentage/points), value input. Default: enabled, 50%, percentage mode. Verify toggling SL on/off shows/hides the config inputs.
- [x] 9.3 Add target profit toggle and configuration: mode switch (percentage/points), value input. Default: disabled. Verify TP config appears only when toggled on.
- [x] 9.4 Add trailing stop loss toggle and value input (points). Default: disabled. Verify it enables only when primary SL is also enabled.
- [x] 9.5 Add VIX filter: optional min and max VIX inputs. Verify empty values mean no filter; filled values include range display.
- [x] 9.6 Add day-of-week filter: 5 checkboxes (Mon-Fri), all checked by default. Verify unchecking a day excludes it from the config object.
- [x] 9.7 Add "Run Backtest" button that calls the parent's onSubmit handler with the assembled BacktestConfig object. Show loading spinner while backtest is running. Disable button during execution. Verify button triggers API call and shows loading state.

## 10. Frontend — Results Dashboard Component

- [x] 10.1 Create `web/frontend/src/components/ResultsDashboard/SummaryStats.tsx` displaying key metrics in a card grid: Total P&L, Trades, Win Rate, Max Drawdown, Sharpe Ratio, Avg Win, Avg Loss, Profit Factor, Max Consecutive Wins/Losses. Verify all metrics render from mock data.
- [x] 10.2 Create `EquityCurve.tsx` using Recharts `LineChart` with date on X-axis and cumulative P&L on Y-axis. Add interactive tooltip showing date and P&L on hover. Verify chart renders with sample equity curve data.
- [x] 10.3 Create `DrawdownChart.tsx` using Recharts `AreaChart` with red fill showing drawdown percentage over time. Verify chart renders below the equity curve with negative-axis values.
- [x] 10.4 Create `MonthlyHeatmap.tsx` displaying a grid (years as rows, months as columns) with P&L values. Green for profit, red for loss, intensity scaled by magnitude. Verify heatmap renders correctly for a 2-year backtest.
- [x] 10.5 Create `TradeLog.tsx` with a sortable, paginated table (20 rows per page) showing all trade records: date, day, entry time, exit time, entry premium, exit premium, P&L, exit reason, cumulative P&L, VIX. Implement column sort (click header) and text search/filter. Verify sorting by P&L and pagination work correctly.
- [x] 10.6 Create `DayOfWeekBreakdown.tsx` using Recharts `BarChart` showing average P&L per weekday. Green for positive, red for negative bars. Verify chart renders from trade data grouped by day of week.
- [x] 10.7 Add CSV export button that downloads the trade log as a CSV file via browser download. Verify the downloaded CSV contains all trade columns and matches displayed data.
- [x] 10.8 Add save backtest button that calls the save API and displays the backtest ID. Verify the saved backtest can be loaded via URL.

## 11. Frontend — Main Backtest Page Assembly

- [x] 11.1 Create `web/frontend/src/pages/Backtest.tsx` composing StrategyBuilder, BacktestConfig, and ResultsDashboard into a single-page layout. Left panel: builder + config. Right panel: results (or placeholder before first run). Verify the page renders all three sections with proper layout.
- [x] 11.2 Create `web/frontend/src/hooks/useBacktest.ts` managing state: strategy definition, config, loading status, results. Handles API call flow: submit → poll status → receive results → update state. Verify the hook orchestrates the full backtest lifecycle.
- [x] 11.3 Wire up the Backtest page: builder changes update strategy state, config changes update config state, Run Backtest triggers the hook, results populate the dashboard. Verify end-to-end: configure strategy → click Run → see results in dashboard.
- [x] 11.4 Add data sync status indicator: on page load, check `/api/data/status` and display available date ranges. If no data synced, show a prompt to sync data first with a sync button. Verify the indicator shows correct cache state and sync triggers correctly.

## 12. Integration Testing & Polish

- [x] 12.1 End-to-end test: start backend, sync 1 month of NIFTY data via API, run a Bull Call Spread backtest for that month, verify results contain correct number of trades and non-zero P&L values. Document the manual test steps and expected results.
- [x] 12.2 Add error handling in the frontend: display API errors (network failure, 401 auth error, 422 validation error) as user-friendly toast notifications. Verify error messages appear for invalid credentials and malformed requests.
- [x] 12.3 Add a startup script (`web/run.py` or `web/__main__.py`) that starts the FastAPI server (with uvicorn) and optionally opens the browser. Verify `python -m web` starts the server and the app is accessible at `http://localhost:8000`.
- [x] 12.4 Update the project root `.gitignore` to exclude `web/cache.db`, `web/frontend/node_modules/`, `web/frontend/dist/`. Verify git status does not show these paths.

## 13. INR Currency + DTE Filter + Real Intraday Pricing (2026-09 follow-up)

- [x] 13.1 Replace `$` with `₹` in every user-facing money label across `web/frontend/src/components/ResultsDashboard/*.tsx` (SummaryStats, EquityCurve, DrawdownChart, DayOfWeekBreakdown, MonthlyHeatmap, TradeLog) — chart axes, tooltips, table cells, and leg-detail rows. Verify the built bundle contains `₹` and no bare `$` in currency positions.
- [x] 13.2 Add `dte: int` and `expiry_date: str` fields to `TradeRecord` in `web/backend/schemas.py` and mirror them (optional) in `web/frontend/src/types/backtest.ts`. Verify Pydantic validation still passes and TypeScript compiles.
- [x] 13.3 Populate `dte` and `expiry_date` in `web/backend/engine/simulator.py` using two helpers: `_infer_weekly_expiry_weekday(fetcher, underlying)` (modal weekday of near-term cached expiries, excluding month-end monthlies) and `_compute_dte(dt, resolved_expiry, weekly_wd, expiry_type)` (use resolved expiry when within 7 days, otherwise project forward to the next occurrence of the inferred weekday). Verify a NIFTY weekly backtest across August 2026 produces a mix of `dte` values including 0 for Tuesdays.
- [x] 13.4 Add a sortable DTE column to `TradeLog.tsx` shown only when trades carry DTE data; 0-DTE rows highlighted orange. Verify the column renders and sorts.
- [x] 13.5 Add a dashboard-level DTE filter dropdown in `ResultsDashboard.tsx` with options `All / 0 DTE (Expiry Day) / 1 / 2 / 3+`. When active, filter `result.trades` and recompute `summary`, `equity_curve`, `drawdown`, `monthly_pnl` and `cumulative_pnl` client-side (matching the backend's `_compute_summary`/`_compute_equity_curve`/`_compute_drawdown`/`_compute_monthly_pnl` formulas). Show a banner "Filtered by DTE = X. Showing N of M trades. All metrics below are recomputed from this subset." Verify equity curve and heatmap re-render for 0-DTE only.
- [x] 13.6 Remove the DTE dropdown from `TradeLog.tsx` after moving it to the dashboard header. Verify only one DTE selector is present in the UI.
- [x] 13.7 Update CSV export in `ResultsDashboard.tsx` to include a `DTE` column and to export only the currently-filtered trades. Verify the downloaded CSV row count matches the filtered trade count.
- [x] 13.8 Add cache helpers in `web/backend/data/cache.py`: `insert_intraday_batch(rows)`, `get_intraday_candle_at_or_after(instrument, dt, strike, option_type, expiry, time_str)`, `has_intraday_data(...)`. Verify a round-trip insert-then-lookup returns the expected candle.
- [x] 13.9 Add `fetch_option_intraday(instrument_key, dt, unit="minutes", interval=5)` on `UpstoxHistoricalFetcher` using the **v3** endpoint `https://api.upstox.com/v3/historical-candle/{key}/{unit}/{interval}/{to}/{from}` (v2 only accepts `1minute`/`30minute`/`day`/`week`/`month`, so it cannot serve 5-min). Returns `[{time: 'HH:MM', open, high, low, close}]` sorted ascending. Verify a call for a known option contract returns ~75 candles for a full trading day.
- [x] 13.10 Add `time_hhmm: Optional[str]` to `get_option_price(...)` in `web/backend/engine/pricing.py`. When provided, ensure intraday is cached (`_ensure_intraday_cached`), look up the candle at or after that time, and return its open price as the leg premium. Fall back to daily open/close, then Black-Scholes, when Upstox intraday is unavailable. Verify the entry leg on 2026-09-11 at 09:20 prices at ~₹111 (real 5-min bar) instead of ₹159.95 (daily open).
- [x] 13.11 Pass `config.entry_time` and `config.exit_time` down to `get_option_price` from the simulator. Verify both entry and exit legs resolve to their respective 5-min bars.
- [x] 13.12 Refactor exit-P&L computation in `simulator.py` so time-exit uses the real per-leg exit prices: `exit_pnl = entry_total + exit_total - 2 * n_legs * lot_size` (slippage matches `_evaluate_strategy_pnl`). Set `exit_premium_display = abs(exit_total)/lot_size` on time-exit and keep `entry + pnl_points` on intraday SL/TP exits. Verify the trade log's Exit Premium column matches the real 5-min bar at exit_time.

## 14. SENSEX Underlying Support

- [x] 14.1 Extend the `Underlying` enum in `web/backend/schemas.py` to include `SENSEX = "SENSEX"`. Verify Pydantic accepts a strategy with `underlying: "SENSEX"`.
- [x] 14.2 Add SENSEX to backend constants in `web/backend/engine/simulator.py` and `web/backend/data/upstox_fetcher.py`: `LOT_SIZES["SENSEX"] = 20`, `STRIKE_INTERVALS["SENSEX"] = 100`, `STRIKE_RANGES["SENSEX"] = 2000`, `INDEX_KEYS["SENSEX"] = "BSE_INDEX|SENSEX"`. Verify a SENSEX sync computes strike list at ATM ± 2000 in 100-point steps.
- [x] 14.3 Extend `fetch_instruments_master()` filter to accept `exchange = "BSE_FO"` when `name == "SENSEX"` (retaining `NSE_FO` for NIFTY/BANKNIFTY). Verify the instruments cache contains a SENSEX weekly expiry after re-fetching.
- [x] 14.4 Update `web/backend/routers/instruments.py` `/api/instruments/underlyings` to include SENSEX metadata `{name: "SENSEX", lot_size: 20, strike_interval: 100, trading_symbol: "SENSEX"}`. Verify GET returns three underlyings.
- [x] 14.5 Update `web/frontend/src/types/backtest.ts` `Underlying` type to `'NIFTY' | 'BANKNIFTY' | 'SENSEX'`. Update `StrategyBuilder.tsx` to render SENSEX in the underlying dropdown. Verify selecting SENSEX drives the strike-interval selection.
- [ ] 14.6 End-to-end test: sync one week of SENSEX data, run a SENSEX ATM straddle backtest for that week, and confirm results appear in the dashboard with SENSEX-appropriate lot size (20) reflected in `total_pnl_inr`. Document the test steps.

## 15. Historical Data + Per-Leg SL + Bhavcopy Backfill (2026-09-13 second follow-up)

- [x] 15.1 Correct NIFTY lot size to 65 in `LOT_SIZES` and `/api/instruments/underlyings`. Update spec scenarios.
- [x] 15.2 Fix summary tiles to display P&L / drawdown / avg win / avg loss in INR (points × lot size), showing points as a small caption. Fix win-rate double-multiplication (`summary.win_rate * 100` → `summary.win_rate`). Fix Trade Log P&L / Cumulative P&L columns to render `pnl_inr` (with pts caption). Fix Leg P&L to multiply by lot_size. Fix Monthly Heatmap and Equity Curve to accept a `lotSize` prop and scale points → INR.
- [x] 15.3 Fix Drawdown chart's `dataKey` from `drawdown` to `drawdown_pct` and change Y-axis / tooltip formatting from ₹ to %.
- [x] 15.4 Add `SLScope` enum (`combined` | `per_leg`) to `BacktestConfig`, default `per_leg`. Add UI toggle in Stop Loss section.
- [x] 15.5 Implement per-leg SL check in `simulator.py`: for each leg, compute SL price from entry premium (SELL: entry × (1+pct); BUY: entry × (1-pct); points mode: entry ± value). Search cached 5-min option candles between entry_time and exit_time; also BS-simulate the intraday journey using cached index 5-min high/low. Stopped legs exit at their SL price; non-stopped legs continue to exit_time. Overall exit_reason = "stop_loss" (all) or "stop_loss (partial)" (some).
- [x] 15.6 Add `index_intraday` table + cache helpers (`insert_index_intraday_batch`, `get_index_intraday_at_or_after`, `has_index_intraday`). Add `fetch_index_intraday(underlying, dt, unit, interval)` on the fetcher (v3 endpoint). Add `_spot_at_entry_time` in simulator that on-demand fetches + caches. ATM is now `round(spot_at_entry_time / interval) * interval` — no longer inherits the 9:15 tick when entry_time > 09:15.
- [x] 15.7 Add rule-based historical expiry calendar `web/backend/data/expiry_calendar.py`: `WEEKLY_EXPIRY_WEEKDAY` per underlying with transition dates, `HOLIDAYS` set, and `historical_expiry(dt, underlying, expiry_type)` with holiday shift-back. Simulator prefers `historical_expiry(dt, ...)` over the live-master `_get_nearest_expiry` result. DTE is `(historical_expiry - dt).days` — always honest.
- [x] 15.8 Add `nse_bhavcopy.py` fetcher with `fetch_bhavcopy(exchange, dt)`, `parse_option_rows(rows, underlyings)`, `fetch_and_parse(dt, underlyings)`. NSE URL: `https://nsearchives.nseindia.com/content/fo/BhavCopy_NSE_FO_0_0_0_{YYYYMMDD}_F_0000.csv.zip`. BSE URL: `https://www.bseindia.com/download/Bhavcopy/Derivative/BhavCopy_BSE_FO_0_0_0_{YYYYMMDD}_F_0000.csv.zip`. Parse handles the modern NSE column schema (`TradDt`, `XpryDt`, `StrkPric`, `OptnTp`, `TckrSymb`, `FinInstrmTp=IDO`, `OpnPric`/`HghPric`/`LwPric`/`ClsPric`, `UndrlygPric`, `FinInstrmId`).
- [x] 15.9 Add `POST /api/data/backfill-bhavcopy` router endpoint that iterates trading days in the requested range, downloads bhavcopy per day, and inserts real daily OHLC into `option_premiums` with the correct historical expiry. Streams progress via the shared task registry, pollable at `/api/data/sync/{task_id}/status`.
- [x] 15.10 Add `implied_vol REAL`, `underlying_close REAL`, and `nse_token INTEGER` columns to `option_premiums`. Migrate the existing DB via `ALTER TABLE`. Update `insert_option_premiums_batch` to persist the new columns.
- [x] 15.11 Add `iv_calibration.py` with a bisection-based reverse-BS solver `implied_vol(spot, strike, market_price, days_to_expiry, option_type, r)`. During bhavcopy backfill, calibrate IV per row using `UndrlygPric` as spot and `ClsPric` as market price; store in `option_premiums.implied_vol`. Return `NULL` for arbitrage-violating rows (market price below intrinsic).
- [x] 15.12 Update pricing engine `get_option_price` to prefer BS-with-calibrated-IV at intraday spot when a `time_hhmm` is set and the row has `implied_vol` — the daily-open shortcut is bypassed in that case since a 15:14 entry shouldn't inherit the 9:15 open. Set `source: "iv_calibrated"` on the return so callers can distinguish real vs calibrated pricing.
- [x] 15.13 Add `fetch_expired_option_intraday(exchange_prefix, token, expiry, dt, interval)` to the Upstox fetcher using `/v2/expired-instruments/historical-candle/{prefix|token|DD-MM-YYYY}/{interval}/{to}/{from}`. Support `1minute`, `30minute`, `day` intervals.
- [x] 15.14 Extend `_ensure_intraday_cached` in `pricing.py` to fall back to `fetch_expired_option_intraday` using `option_premiums.nse_token` when the live-master lookup misses. Downsample 1-min bars to 5-min buckets via `_bucket_1m_to_5m` to keep parity with the existing cache format. Insert into `intraday_candles` as usual.
- [x] 15.15 Extend `POST /api/data/backfill-bhavcopy` to also prefetch, for each backfilled trading day: the day's index 5-min intraday, plus 5-min intraday for ATM ± 5 strikes × CE/PE across the 2 nearest expiries. Uses `fetch_expired_option_intraday`. Any fetch failure counts as "skipped" and falls through to BS with calibrated IV — no behaviour regression. Progress message: `{date}: {N} daily rows | intraday cached: {ok} ok, {failed} skipped`.
- [x] 15.16 Add UI Data Status panel: per-underlying row (NIFTY / BANKNIFTY / SENSEX) with "current" / "N trading days behind" indicator (weekend-aware), row-level "Catch up" button, top-level "Catch up all" that sequentially catches up each underlying. Add collapsible "Fetch older…" panel with preset ranges (Last 3m / 6m / 1y / 2y / 3y / 5y), custom date-range picker, and two action buttons: "Fetch (Upstox)" (live-master path) and "Backfill (Bhavcopy)" (historical/expired path). Sync-in-progress state locks all data buttons; per-underlying % progress shown.
- [x] 15.17 Narrow the Upstox sync's strike window from ATM ± 1000/2000 pts to `STRIKE_WINDOW = 2` strikes (5 strikes × CE/PE = 10 daily calls per date). ~8× faster catch-up sync. Documented in the UI as "sync range: ATM ± 2 strikes".
- [x] 15.18 Purge existing `saved_backtests` rows via a manual `DELETE FROM saved_backtests` because their strike selections were computed against the pre-fix ATM formula and would confuse users.
- [ ] 15.19 End-to-end verification: run a NIFTY 1-year bhavcopy backfill via the UI, confirm each trading day gets both daily premiums + 5-min intraday for the nearest 2 expiries, then run a short-straddle per-leg-SL backtest across historical Tuesdays and verify `source: "real"` / `source: "stop_loss"` labels appear on the leg-details rows (i.e. no BS fallback needed). Document expected wall-clock time (~30–60 min per underlying for a year).
